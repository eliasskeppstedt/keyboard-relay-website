import { type VKC } from '../code.help.ts';

export const VK_EXTRAS: Record<string, VKC> = { // OBS: constants.ts -> UNSUPPORTED_KEY also 300
    BLOCK: {
        code: "BLOCK",
        eCode: 1,
        keyType: "EC",
        legend: '🚫',
        description: "Blocked",
    }
};
